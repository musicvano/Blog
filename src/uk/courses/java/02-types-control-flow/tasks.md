---
title: "Завдання"
description: "Тема 2. Типи, операції, керування: варіанти завдань"
outline: [2, 3]
---

# Завдання

Відповідно до номера свого варіанта виконайте завдання обраного рівня складності.

## Варіанти

### Варіант 1. Калькулятор ПДВ {#v1}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати суму 0..1000000 та команду add/remove/exit; навчальна ставка 20 відсотків, add=s\*1.2, remove=s/1.2; вивести базу, податок і підсумок. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати суму 0..1000000 та команду add/remove/exit; навчальна ставка 20 відсотків, add=s\*1.2, remove=s/1.2; вивести базу, податок і підсумок. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати суму 0..1000000 та команду add/remove/exit; навчальна ставка 20 відсотків, add=s\*1.2, remove=s/1.2; вивести базу, податок і підсумок. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 2. Вгадай число {#v2}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: задати секрет 1..100, читати до 7 спроб, повідомляти більше/менше/вгадано; неправильний токен не витрачає спробу, EOF завершує гру. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: задати секрет 1..100, читати до 7 спроб, повідомляти більше/менше/вгадано; неправильний токен не витрачає спробу, EOF завершує гру. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: задати секрет 1..100, читати до 7 спроб, повідомляти більше/менше/вгадано; неправильний токен не витрачає спробу, EOF завершує гру. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 3. Високосні роки {#v3}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати межі a,b 1..9999 з a&lt;=b; вивести високосні роки за правилом кратності 400 або 4 без 100 та їх кількість. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати межі a,b 1..9999 з a&lt;=b; вивести високосні роки за правилом кратності 400 або 4 без 100 та їх кількість. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати межі a,b 1..9999 з a&lt;=b; вивести високосні роки за правилом кратності 400 або 4 без 100 та їх кількість. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 4. Системи числення {#v4}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати ціле n 0..65535 та основу 2, 8, 16; вивести подання n у вибраній основі й кількість одиничних бітів. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати ціле n 0..65535 та основу 2, 8, 16; вивести подання n у вибраній основі й кількість одиничних бітів. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати ціле n 0..65535 та основу 2, 8, 16; вивести подання n у вибраній основі й кількість одиничних бітів. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 5. Каса {#v5}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати суму 0..100000, кратну 10; видати мінімальне число купюр 100, 50, 20, 10 і перевірити відновлення суми. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати суму 0..100000, кратну 10; видати мінімальне число купюр 100, 50, 20, 10 і перевірити відновлення суми. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати суму 0..100000, кратну 10; видати мінімальне число купюр 100, 50, 20, 10 і перевірити відновлення суми. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 6. Таблиця множення {#v6}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати n 1..20; вивести вирівняну таблицю добутків 1..n та суму кожного рядка. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати n 1..20; вивести вирівняну таблицю добутків 1..n та суму кожного рядка. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати n 1..20; вивести вирівняну таблицю добутків 1..n та суму кожного рядка. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 7. Прості числа {#v7}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати a,b 2..100000 з a&lt;=b і b-a&lt;=10000; вивести прості числа та число пар із різницею 2; перевіряти дільники до d&lt;=n/d. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати a,b 2..100000 з a&lt;=b і b-a&lt;=10000; вивести прості числа та число пар із різницею 2; перевіряти дільники до d&lt;=n/d. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати a,b 2..100000 з a&lt;=b і b-a&lt;=10000; вивести прості числа та число пар із різницею 2; перевіряти дільники до d&lt;=n/d. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 8. Банкомат {#v8}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: початковий баланс 10000 коп, навчальний PIN 2468, три спроби; після входу команди balance/withdraw/exit, сума 1..10000 не перевищує залишок; вивести незмінний баланс після відмови. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: початковий баланс 10000 коп, навчальний PIN 2468, три спроби; після входу команди balance/withdraw/exit, сума 1..10000 не перевищує залишок; вивести незмінний баланс після відмови. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: початковий баланс 10000 коп, навчальний PIN 2468, три спроби; після входу команди balance/withdraw/exit, сума 1..10000 не перевищує залишок; вивести незмінний баланс після відмови. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 9. Шкала ECTS {#v9}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати цілі бали 0..100; локальне правило A90..100,B82..89,C74..81,D64..73,E60..63,F0..59; вивести літеру й кількість оброблених балів. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати цілі бали 0..100; локальне правило A90..100,B82..89,C74..81,D64..73,E60..63,F0..59; вивести літеру й кількість оброблених балів. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати цілі бали 0..100; локальне правило A90..100,B82..89,C74..81,D64..73,E60..63,F0..59; вивести літеру й кількість оброблених балів. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 10. Календарні інтервали {#v10}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати номер місяця 1..12 і рік 1..9999; вивести кількість днів із перевіркою високосності та номер кварталу, без астрологічних тверджень. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати номер місяця 1..12 і рік 1..9999; вивести кількість днів із перевіркою високосності та номер кварталу, без астрологічних тверджень. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати номер місяця 1..12 і рік 1..9999; вивести кількість днів із перевіркою високосності та номер кварталу, без астрологічних тверджень. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 11. День тижня {#v11}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати n днів від відомого понеділка 0..1000000; вивести день тижня за n%7 та число повних тижнів. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати n днів від відомого понеділка 0..1000000; вивести день тижня за n%7 та число повних тижнів. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати n днів від відомого понеділка 0..1000000; вивести день тижня за n%7 та число повних тижнів. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 12. Навчальний податок {#v12}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати до EOF невід’ємні доходи 0..1000000; задані умовні ставки 18 і 5 відсотків, вивести обидві суми та залишок; не трактувати ставки як чинне законодавство. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати до EOF невід’ємні доходи 0..1000000; задані умовні ставки 18 і 5 відсотків, вивести обидві суми та залишок; не трактувати ставки як чинне законодавство. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати до EOF невід’ємні доходи 0..1000000; задані умовні ставки 18 і 5 відсотків, вивести обидві суми та залишок; не трактувати ставки як чинне законодавство. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 13. Умовні зони показника {#v13}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати максимум m 1..1000 та відсоткові межі 50, 60, 70, 80, 90; вивести навчальну таблицю m\*p/100 без медичних порад. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати максимум m 1..1000 та відсоткові межі 50, 60, 70, 80, 90; вивести навчальну таблицю m\*p/100 без медичних порад. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати максимум m 1..1000 та відсоткові межі 50, 60, 70, 80, 90; вивести навчальну таблицю m\*p/100 без медичних порад. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 14. Ряд Тейлора {#v14}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати x від -5 до 5 та epsilon 1e-12..1e-3; обчислити e^x сумою членів t\*=x/k до abs(t)&lt;epsilon або 100 членів; вивести суму й число членів. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати x від -5 до 5 та epsilon 1e-12..1e-3; обчислити e^x сумою членів t\*=x/k до abs(t)&lt;epsilon або 100 членів; вивести суму й число членів. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати x від -5 до 5 та epsilon 1e-12..1e-3; обчислити e^x сумою членів t\*=x/k до abs(t)&lt;epsilon або 100 членів; вивести суму й число членів. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 15. Фібоначчі {#v15}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати n 0..92; вивести F0=0,F1=1 і наступні значення до Fn у long, перевіряючи переповнення перед додаванням. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати n 0..92; вивести F0=0,F1=1 і наступні значення до Fn у long, перевіряючи переповнення перед додаванням. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати n 0..92; вивести F0=0,F1=1 і наступні значення до Fn у long, перевіряючи переповнення перед додаванням. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 16. Цифри числа {#v16}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати n 0..1000000000; вивести суму цифр, число цифр та ознаку числового паліндрому; нуль має одну цифру. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати n 0..1000000000; вивести суму цифр, число цифр та ознаку числового паліндрому; нуль має одну цифру. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати n 0..1000000000; вивести суму цифр, число цифр та ознаку числового паліндрому; нуль має одну цифру. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 17. Кавовий автомат {#v17}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: меню чай 30, кава 40, какао 50 грн; читати вибір та оплату 0..1000; відмовити при нестачі, інакше вивести решту; exit завершує. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: меню чай 30, кава 40, какао 50 грн; читати вибір та оплату 0..1000; відмовити при нестачі, інакше вивести решту; exit завершує. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: меню чай 30, кава 40, какао 50 грн; читати вибір та оплату 0..1000; відмовити при нестачі, інакше вивести решту; exit завершує. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 18. Таксі {#v18}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати кілометри 0..1000, хвилини очікування 0..1440 та night true/false; навчальний тариф 50+12\*км+2\*хв, уночі множник 1.2; вивести деталізацію. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати кілометри 0..1000, хвилини очікування 0..1440 та night true/false; навчальний тариф 50+12\*км+2\*хв, уночі множник 1.2; вивести деталізацію. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати кілометри 0..1000, хвилини очікування 0..1440 та night true/false; навчальний тариф 50+12\*км+2\*хв, уночі множник 1.2; вивести деталізацію. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 19. Трикутник Паскаля {#v19}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати n 1..20; вивести n рядків, обчислюючи наступний коефіцієнт рядка c\*(row-k)/(k+1) у long. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати n 1..20; вивести n рядків, обчислюючи наступний коефіцієнт рядка c\*(row-k)/(k+1) у long. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати n 1..20; вивести n рядків, обчислюючи наступний коефіцієнт рядка c\*(row-k)/(k+1) у long. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 20. Лінійна шкала температур {#v20}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати a,b від -100 до 100 з a&lt;=b та цілий крок 1..20; вивести Celsius і Fahrenheit=1.8\*C+32, не давати рекомендацій щодо одягу. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати a,b від -100 до 100 з a&lt;=b та цілий крок 1..20; вивести Celsius і Fahrenheit=1.8\*C+32, не давати рекомендацій щодо одягу. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати a,b від -100 до 100 з a&lt;=b та цілий крок 1..20; вивести Celsius і Fahrenheit=1.8\*C+32, не давати рекомендацій щодо одягу. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 21. Шаховий кінь {#v21}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати дві цілі координати x,y 1..8; перебрати зміщення від -2 до 2 з abs(dx)\*abs(dy)==2, вивести лише клітинки дошки. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати дві цілі координати x,y 1..8; перебрати зміщення від -2 до 2 з abs(dx)\*abs(dy)==2, вивести лише клітинки дошки. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати дві цілі координати x,y 1..8; перебрати зміщення від -2 до 2 з abs(dx)\*abs(dy)==2, вивести лише клітинки дошки. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 22. НСД і НСК {#v22}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати a,b 1..1000000; алгоритмом Евкліда знайти НСД, НСК=(a/НСД)\*b у long; вивести також скорочений дріб. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати a,b 1..1000000; алгоритмом Евкліда знайти НСД, НСК=(a/НСД)\*b у long; вивести також скорочений дріб. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати a,b 1..1000000; алгоритмом Евкліда знайти НСД, НСК=(a/НСД)\*b у long; вивести також скорочений дріб. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 23. Депозит із поповненням {#v23}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати початкову суму 0..1000000, місячну ставку 0..0.05, поповнення 0..100000 та місяці 1..120; щомісяця balance=balance\*(1+r)+add; вивести навчальну таблицю. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати початкову суму 0..1000000, місячну ставку 0..0.05, поповнення 0..100000 та місяці 1..120; щомісяця balance=balance\*(1+r)+add; вивести навчальну таблицю. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати початкову суму 0..1000000, місячну ставку 0..0.05, поповнення 0..100000 та місяці 1..120; щомісяця balance=balance\*(1+r)+add; вивести навчальну таблицю. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 24. Ліфт {#v24}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: початковий поверх 1; читати цілі виклики 1..20 до 0; час переміщення 3 секунди на поверх плюс 5 на зупинку; вивести маршрут і сумарний час. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: початковий поверх 1; читати цілі виклики 1..20 до 0; час переміщення 3 секунди на поверх плюс 5 на зупинку; вивести маршрут і сумарний час. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: початковий поверх 1; читати цілі виклики 1..20 до 0; час переміщення 3 секунди на поверх плюс 5 на зупинку; вивести маршрут і сумарний час. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 25. Фігури із символів {#v25}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати непарне n 1..39; вивести ромб висоти n із символів \*, визначаючи відступ і ширину кожного рядка циклами. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати непарне n 1..39; вивести ромб висоти n із символів \*, визначаючи відступ і ширину кожного рядка циклами. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати непарне n 1..39; вивести ромб висоти n із символів \*, визначаючи відступ і ширину кожного рядка циклами. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 26. Тренажер рахунку {#v26}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: для k 1..10 ставити приклади k+(11-k); читати цілі відповіді, рахувати правильні та неправильні, EOF завершує з підсумком. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: для k 1..10 ставити приклади k+(11-k); читати цілі відповіді, рахувати правильні та неправильні, EOF завершує з підсумком. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: для k 1..10 ставити приклади k+(11-k); читати цілі відповіді, рахувати правильні та неправильні, EOF завершує з підсумком. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 27. Тривалість у секундах {#v27}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати t 0..1000000000; вивести повні тижні, дні, години, хвилини, секунди та перевірити відновлення t. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати t 0..1000000000; вивести повні тижні, дні, години, хвилини, секунди та перевірити відновлення t. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати t 0..1000000000; вивести повні тижні, дні, години, хвилини, секунди та перевірити відновлення t. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 28. Розклад дзвінків {#v28}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати початок h 0..23, m 0..59, число занять 1..10, тривалість 1..120 хв і перерву 0..60 хв; вивести початок/кінець кожного заняття з номером доби. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати початок h 0..23, m 0..59, число занять 1..10, тривалість 1..120 хв і перерву 0..60 хв; вивести початок/кінець кожного заняття з номером доби. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати початок h 0..23, m 0..59, число занять 1..10, тривалість 1..120 хв і перерву 0..60 хв; вивести початок/кінець кожного заняття з номером доби. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 29. Римські числа {#v29}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати n 1..10; switch-виразом вивести I, II, III, IV, V, VI, VII, VIII, IX, X; інші значення відхилити. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати n 1..10; switch-виразом вивести I, II, III, IV, V, VI, VII, VIII, IX, X; інші значення відхилити. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати n 1..10; switch-виразом вивести I, II, III, IV, V, VI, VII, VIII, IX, X; інші значення відхилити. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

### Варіант 30. Лічильник води {#v30}

**1. Початковий рівень.** Створити консольну програму Java на JDK 27: читати послідовні невід’ємні показники до EOF, кожен не менший за попередній; тариф 20 грн/м³, вивести споживання та рахунок кожного інтервалу, відмови не змінюють останній показник. Застосувати відповідні розгалуження та цикли; неправильний ввід пояснити без неправильного підсумку.

**2. Базовий рівень.** Створити консольну програму Java на JDK 27: читати послідовні невід’ємні показники до EOF, кожен не менший за попередній; тариф 20 грн/м³, вивести споживання та рахунок кожного інтервалу, відмови не змінюють останній показник. Після неправильного рядка повторювати ввід, EOF обробити явно; вести число успішних обчислень, перевірити обидві межі й порожній потік.

**3. Високий рівень.** Створити консольну програму Java на JDK 27: читати послідовні невід’ємні показники до EOF, кожен не менший за попередній; тариф 20 грн/м³, вивести споживання та рахунок кожного інтервалу, відмови не змінюють останній показник. Організувати повторюване меню з виходом; початкові параметри читати з CLI як ключ=значення, без аргументів із клавіатури; довести завершення циклів. Додати `--help`, перевірку формату, скінченності чисел і меж; помилки друкувати в stderr, коди 0 – успіх/довідка, 2 – неправильний ввід, 1 – операційний збій. Вивести вирівняний звіт із підсумком. Подати не менше п’яти контрольних випадків, README й локальну історію Git.

## Порядок виконання та захисту роботи

1. Запишіть діапазони та формат вхідних даних.
2. Оберіть типи із запасом для проміжних результатів.
3. Побудуйте перевірку формату, меж та EOF.
4. Поясніть умову завершення кожного циклу.
5. Порівняйте результат із ручним обчисленням.
6. Перевірте неправильний токен, нуль та обидві межі.
7. Подайте вихідний код, README й історію Git.
